import React from 'react'
import styles from './Card.module.css'

const Card = ({ movie }) => {
  const bookShow = () => {
    
  }
  return (
    <div>
      <p>Movie Card</p>
    </div>
  )
}

export default Card
